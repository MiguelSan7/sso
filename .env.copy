# URL del backend (AdonisJS) para llamadas servidor-servidor
BACKEND_API_URL=http://localhost:3333

# URL del backend para llamadas desde el navegador
PUBLIC_API_URL=http://localhost:3333

# URL de este mismo proyecto (Astro SSO)
PUBLIC_SSO_URL=http://localhost:4321

# URL del Panel Web (React/Vue/Astro) a donde redirigirá el SSO por defecto al entrar a la raíz
PUBLIC_PANEL_URL=http://localhost:5173
